 <view class="top">
    <view>
      <view class="top_middle">
        <image style="border-radius:50%;" src="{{'/images/user/user1.png'}}"></image>
      </view>
      <view class="text">
        <text bindtap="register">注册 / 登录</text>
      </view>
    </view>
  </view>
  
  <view>
      <block wx:for="{{items}}" wx:key="items_for">
        <navigator url="{{item.url}}" class="lists">
        <view class='content'>
          <view class="listimg">
            <image src="{{item.img}}"></image>
          </view>
          <view class="listtext">{{item.text}}</view>
        </view>
 </navigator>
        <view class="line"></view>
      </block>
  </view>